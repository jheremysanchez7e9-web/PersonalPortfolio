/* VARIABLES PARA TEMAS (Oscuro por defecto) */
:root {
    --bg: #0f172a;
    --container-bg: #1e293b;
    --card: #334155;
    --border: #475569;
    --text: #f8fafc;
    --accent: #38bdf8;
    --accent-hover: #7dd3fc;
    --btn-text: #0f172a;
}

/* TEMA CLARO */
[data-theme="light"] {
    --bg: #f1f5f9;
    --container-bg: #ffffff;
    --card: #f8fafc;
    --border: #cbd5e1;
    --text: #1e293b;
    --accent: #2563eb; /* Azul corporativo más oscuro para mejor contraste */
    --accent-hover: #1d4ed8;
    --btn-text: #ffffff;
}

* { box-sizing: border-box; margin: 0; padding: 0; transition: background-color 0.3s, color 0.3s, border-color 0.3s; }

body { 
    background: var(--bg); color: var(--text); 
    font-family: system-ui, -apple-system, sans-serif;
    height: 100vh; display: flex; align-items: center; justify-content: center;
    overflow: hidden;
}

.container {
    width: 95%; max-width: 1100px; height: 85vh;
    background: var(--container-bg); border: 1px solid var(--border);
    border-radius: 12px; padding: 30px;
    display: grid; grid-template-rows: auto 1fr auto;
    box-shadow: 0 25px 50px -12px rgba(0,0,0,0.3);
}

.header { display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid var(--border); padding-bottom: 15px; }
.brand h1 { font-size: 1.4rem; letter-spacing: 1px; }
.brand span { font-family: monospace; color: var(--accent); font-size: 0.9rem; }

/* Controles Superiores */
.top-controls { display: flex; align-items: center; gap: 20px; }
.icon-btn { background: none; border: none; color: var(--accent); font-size: 1.2rem; cursor: pointer; padding: 5px; }
.icon-btn:hover { color: var(--accent-hover); transform: scale(1.1); }
.lang-control { display: flex; align-items: center; gap: 8px; color: var(--accent); }
#lang-select { background: transparent; color: var(--text); border: 1px solid var(--border); padding: 5px; border-radius: 4px; outline: none; cursor: pointer; }
#lang-select option { background: var(--container-bg); color: var(--text); }

.main-content { overflow-y: auto; padding: 30px 0; scrollbar-width: thin; }
.view-section { display: none; flex-direction: column; animation: fadeIn 0.4s ease; }
@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

/* Home */
#home-view { text-align: center; justify-content: center; min-height: 100%; }
.hero-title { font-size: 2.8rem; margin-bottom: 15px; font-weight: 800; }
.hero-desc { max-width: 650px; margin: 0 auto 30px; opacity: 0.8; line-height: 1.6; font-size: 1.1rem; }
.hero-btns { display: flex; gap: 15px; justify-content: center; }

/* Sobre Mi */
.about-card { background: var(--card); border: 1px solid var(--border); border-radius: 12px; padding: 30px; max-width: 800px; margin: 0 auto; }
.about-text { font-size: 1.1rem; line-height: 1.7; margin-bottom: 15px; opacity: 0.9; }
.skills-tags { display: flex; gap: 10px; flex-wrap: wrap; margin-top: 25px; }
.skills-tags span { background: var(--container-bg); border: 1px solid var(--accent); color: var(--accent); padding: 8px 15px; border-radius: 6px; font-size: 0.9rem; font-weight: bold; }

/* Proyectos */
.grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; }
.project-card { background: var(--card); border: 1px solid var(--border); padding: 25px; border-radius: 12px; display: flex; flex-direction: column; gap: 10px; }
.project-card h3 { color: var(--accent); font-size: 1.2rem; }
.project-card p { opacity: 0.8; line-height: 1.5; margin-bottom: 10px; }

.btn-link { text-decoration: none; color: var(--btn-text); background: var(--border); padding: 10px 15px; border-radius: 6px; font-size: 0.85rem; display: inline-flex; align-items: center; gap: 8px; width: fit-content; font-weight: bold; }
.btn-link:hover { background: var(--accent); color: var(--btn-text); }
.disabled { opacity: 0.5; pointer-events: none; }

/* Contacto */
.contact-layout { display: grid; grid-template-columns: 1fr 1fr; gap: 30px; }
.info-card { display: flex; align-items: center; gap: 15px; text-decoration: none; color: var(--text); padding: 15px; background: var(--card); border-radius: 8px; margin-bottom: 12px; border: 1px solid var(--border); }
.info-card i { font-size: 1.5rem; color: var(--accent); }
.info-card:hover { border-color: var(--accent); }

input, textarea { width: 100%; padding: 12px; margin-bottom: 12px; background: var(--bg); border: 1px solid var(--border); color: var(--text); border-radius: 6px; font-family: inherit; }
input::placeholder, textarea::placeholder { color: var(--text); opacity: 0.6; }

.btn-primary { background: var(--accent); color: var(--btn-text); border: none; padding: 12px 25px; border-radius: 6px; font-weight: 700; cursor: pointer; letter-spacing: 1px; }
.btn-outline { background: transparent; border: 2px solid var(--accent); color: var(--text); padding: 12px 25px; border-radius: 6px; cursor: pointer; font-weight: 700; }
.btn-primary:hover, .btn-outline:hover { background: var(--accent-hover); color: var(--btn-text); border-color: var(--accent-hover); }

.map-box { border-radius: 12px; overflow: hidden; border: 1px solid var(--border); height: 350px; }

/* Footer */
.footer { display: flex; justify-content: center; border-top: 1px solid var(--border); padding-top: 20px; }
.nav-link { background: none; border: none; color: var(--text); padding: 10px 20px; cursor: pointer; opacity: 0.5; font-weight: bold; }
.nav-link.active { opacity: 1; color: var(--accent); border-bottom: 2px solid var(--accent); }

@media (max-width: 850px) { .contact-layout { grid-template-columns: 1fr; } .container { height: 95vh; } }
